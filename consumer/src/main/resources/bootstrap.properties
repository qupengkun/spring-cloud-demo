#应用名，需要与git仓库中{application}一致
spring.application.name = consumer
#config server 地址
spring.cloud.config.uri=http://localhost:9999
#{profile}名（环境）
spring.cloud.config.profile=dev
#{label}名，git仓库分支
spring.cloud.config.label=master

